# Sample Prompts

## Generate Formula Mode
- Create a form with Name and Capacity fields.
- Create a booking form with Title, Start Date, End Date, and IsActive checkbox.
- Build a PowerApps Patch formula to save customer records into Customers list.
- Create a maintenance request app with fields: Asset ID, Priority, Description, Requested Date.

## Fix PowerApps Error Mode
- Fix this error: "Incompatible types for comparison" in If(IsBlank(TextInput1.Text), 0, TextInput1.Text).
- Fix the Patch formula error where Capacity is text but SharePoint expects a number.
- Correct delegation warning in this Filter formula for large datasets.
- Repair formula: Patch(Assets, Defaults(Assets), {Name: txtName.Text, Capacity: txtCap.Text}) with type mismatch.
